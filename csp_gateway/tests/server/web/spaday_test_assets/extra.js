// Test fixture: a component package needs a real asset to serve, and this stands in for a
// deployment's own element library. It registers nothing; the tests assert that it is linked
// into the page and fetchable, not what it does.
export const CSP_GATEWAY_TEST_EXTRA = true;
