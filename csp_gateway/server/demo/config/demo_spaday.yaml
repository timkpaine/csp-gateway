# @package _global_
defaults:
  - /gateway: demo
  - _self_

# csp-gateway-start --config-dir=csp_gateway/server/demo +config=demo_spaday
#
# Same as the `demo` config, but serves the spaday-based UI instead of the built-in
# Perspective/React UI. Requires the `spaday` optional dependency.

port: 8000

gateway:
  settings:
    UI_PROVIDER: spaday
